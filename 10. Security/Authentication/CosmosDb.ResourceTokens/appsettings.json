{
    // The user application needs only the endpoint without the master key, using a resource token based on the user's permissions
    "CosmosEndpoint": "[ENDPOINT]",

    // The master key is needed only by the administrative application that creates users and permissions
    "CosmosMasterKey": "[MASTER-KEY]"
}
